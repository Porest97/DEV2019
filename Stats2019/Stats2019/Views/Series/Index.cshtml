@model IEnumerable<Stats2019.Models.Series>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SeriesName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MatchLength)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RefFee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LMFee)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SeriesName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MatchLength)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RefFee)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LMFee)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
